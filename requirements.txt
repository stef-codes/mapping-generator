streamlit>=1.32.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
# Optional for local Windows SQL Server (Live database tab).
# Do NOT install on Streamlit Cloud — pyodbc can segfault without ODBC drivers.
# pip install "pyodbc>=5.0.0"
# Gemini uses HTTPS REST (stdlib urllib) — no google-genai / grpc packages.
